# Spring Application Configuration
spring.application.name=pointtils-api-test
server.port=0

# Test Database Configuration (H2 in-memory)
spring.datasource.url=jdbc:h2:mem:testdb;MODE=PostgreSQL;
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=create-drop

spring.jpa.show-sql=false

# JWT Configuration for tests
security.jwt.secret-key=dGVzdHNlY3JldGtleWZvcnRlc3RzMTIzNDU2Nzg5MDEyMzQ1Njc4OTAxMjM0
security.jwt.expiration-time=900000
security.jwt.refresh-expiration-time=86400000

# Flyway Configuration (disabled for tests since we use H2 with create-drop)
spring.flyway.enabled=false

# Swagger/OpenAPI Configuration (disabled for tests)
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false
springdoc.swagger-ui.path=/swagger-ui.html

# Clientes HTTP
client.ibge.state-url=http://exemplo.com.br/estados
client.ibge.city-url=http://exemplo.com.br/estados/{state}/municipios

cloud.aws.region.static=us-east-2
aws.region=us-east-2