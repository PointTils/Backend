# Exemplo de valores para as variáveis (NÃO incluir em repositórios públicos)
# Este arquivo deve ser substituído pelas variáveis do GitHub Actions durante o deploy
aws_region     = "us-east-2"
db_username    = "pointtilsadmin"
db_password    = "pointtilspass"  # Mude para uma senha mais segura em produção
db_name        = "pointtils-db"
jwt_secret     = "testandoUmaNovaSenhaMasterComMaisDeTrintaEdoisCaracteres"  # Mude para uma chave segura em produção
